words = ["Hello", "World", "Spam", "eggs"]
for word in words:
    print (word + "!")
    
#counting the number of words
    str = "testing for loop"
    count = 0
    for X in str:
        if(X == "t"):
            count += 1
    
print(count)